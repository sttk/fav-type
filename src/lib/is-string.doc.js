/**
@public
@memberof fav.type
@function isString
@param {any} value The value to be checked.
@return {boolean} True, if *value* is a string or a `String` object.
@desc
Checks if *value* is a string or a `String` object.
*/
